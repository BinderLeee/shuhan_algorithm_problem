防晒
#### 链接：https://www.acwing.com/problem/content/112/
#### 难度：普及
#### tags：贪心
i头牛的min,max可以看作是线段，贪心方法是:每一条线段按照右端点从大到小排序，如果右端点相同则左端点从大到小排序，防晒霜选择能满足条件的最大的。
证明防晒霜要选择满足条件的最大的:对于两条线段(x1,y1),(x2,y2)其中y1 >= y2。设(x1,y1)上有两个点a,b(a<b)。
这样会出现三种情况：1.第二条线段a,b都没有包含，那选a选b都一样
2.第二条线段包含a不包含b，那第一条线段应该选b这样a可以留给第二条线段
3.第二条线段既包含a又包含b，那选a选b都一样
